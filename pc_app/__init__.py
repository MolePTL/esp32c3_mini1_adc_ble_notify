"""pc_app 包入口说明。

这个文件本身几乎没有业务逻辑，但它在 Python 工程结构里依然有明确作用：

1. 让解释器把 `pc_app` 识别成一个“包”（package）
   只有成为包之后，像 `from pc_app.main_window import MainWindow`
   这样的绝对导入才有统一的模块边界。

2. 让项目支持 `python -m pc_app.main` 这种模块方式启动
   这比直接双击某个 `.py` 文件更接近一个完整工程的运行方式。

3. 从教学角度上，它明确告诉读者：
   `pc_app` 不是一堆零散脚本，而是一个按模块划分的桌面应用工程。

虽然这里只保留了文档字符串，没有定义函数或类，
但它依然是理解项目结构的第一层入口。
"""
